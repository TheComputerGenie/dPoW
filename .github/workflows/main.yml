name: CI

on: [push, pull_request]
jobs:
  build:
    name: Build dPoW client "iguana" on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, ubuntu-22.04]
    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: |
        sudo apt-get update && sudo apt-get upgrade -y
        sudo apt-get install libsodium-dev g++-multilib libncurses-dev zlib1g-dev libssl-dev libprotobuf-dev protobuf-compiler libgtest-dev libdb++-dev software-properties-common libevent-dev libcurl4-gnutls-dev jq -y
    - name: Install nanomsg
      run: |
        git clone https://github.com/nanomsg/nanomsg
        cd nanomsg
        cmake . -DNN_TESTS=OFF -DNN_ENABLE_DOC=OFF
        make -j2
        sudo make install
        sudo ldconfig
    - name: Build dpow client "iguana"
      run: cd iguana && make
